[build]
  base = "frontend/"
  command = "npm run build"
  publish = "dist"

# SPA routing - all paths serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
