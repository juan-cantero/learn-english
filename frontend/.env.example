# LearnTV Frontend Environment Variables
# Copy to .env.local for local dev, or set in Netlify dashboard for production.
# NEVER commit .env.local to git!

# === Required for production ===

# Backend API URL (your Render service URL)
VITE_API_BASE_URL=https://your-backend.onrender.com/api/v1

# Supabase (from your Supabase project dashboard > Settings > API)
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...your-anon-key

# Note: The anon key is safe to expose in the frontend.
# It only allows operations permitted by your Row Level Security policies.
# NEVER put the service_role key here!
